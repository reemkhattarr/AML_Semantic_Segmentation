# core libs
numpy>=1.26,<3
opencv-python>=4.7
matplotlib>=3.7,<3.10
albumentations>=1.4
easydict>=1.9
tqdm>=4.65
pyyaml>=6.0
pillow>=10.0
scipy>=1.10
scikit-image>=0.22

# profiling (opzionale)
thop>=0.1.1.post2209072238

# onnx & runtime (opzionali)
onnx>=1.15
onnxruntime-gpu>=1.18 ; platform_system!="Windows"

# protobuf (solo se ti serve pin esplicito — altrimenti ometti)
# protobuf>=4.25,<6
